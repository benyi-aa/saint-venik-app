{
  "name": "saint-venik-app",
  "version": "1.0.0",
  "private": true,
  "description": "App de Saint Venik: bloques de tema (selector de color, selector de talla, guia de tallas). Sin backend.",
  "license": "UNLICENSED",
  "scripts": {
    "link": "shopify app config link",
    "dev": "shopify app dev",
    "deploy": "shopify app deploy",
    "info": "shopify app info",
    "version": "node scripts/version.mjs",
    "revisar": "node scripts/revisar.mjs",
    "publicar": "node scripts/version.mjs && node scripts/revisar.mjs"
  },
  "devDependencies": {
    "@shopify/cli": "^4.8.0",
    "acorn": "^8.18.0",
    "acorn-walk": "^8.3.5"
  }
}
